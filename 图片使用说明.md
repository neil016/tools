# 天天答题王 - 图片使用说明

## 📷 如何添加您的图片

### 方案 1: 使用 public 文件夹（推荐）

1. 将您上传的第一张图片（校园背景图）保存为：
   `/workspace/public/background.jpg`

2. 将您上传的第二张图片（卡通男孩）保存为：
   `/workspace/public/character.png`

3. 修改首页文件中的引用路径：
   `/workspace/src/pages/Home.vue` 中的图片引用改为：

   ```javascript
   const backgroundImage = ref('/background.jpg')
   const characterImage = ref('/character.png')
   ```

### 方案 2: 使用 src/assets 文件夹

1. 将图片放入 `/workspace/src/assets/` 文件夹

2. 直接通过 import 引用图片

### 🎯 当前状态

- 页面结构已经完善
- 功能都正常
- 只要把图片放进去就能完美显示！
